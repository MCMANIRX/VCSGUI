import java.awt.BorderLayout;
import java.io.File;
import java.io.IOException;

import javax.swing.JFileChooser;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JPanel;
import javax.swing.filechooser.FileNameExtensionFilter;

import com.sun.xml.internal.ws.org.objectweb.asm.Label;

public class iso {
	
/*	JFrame frame = new JFrame();
	public JFileChooser fc = new JFileChooser();
	String name;
	GUI getname;
	
	public iso() throws IOException {
		
		  frame.add(fc);
	        fc.setDialogTitle("Select KAR .iso");
		FileNameExtensionFilter	ff = new FileNameExtensionFilter("Air Ride .iso", "iso");
		File workingDirectory = new File(System.getProperty("user.dir"));
		fc.setCurrentDirectory(workingDirectory);
		fc.addChoosableFileFilter (ff);
		fc.setFileFilter(ff);
	
		
		
	     
			
		      
		        
		    	int result = fc.showOpenDialog(frame);
		switch (result) {
	    case JFileChooser.APPROVE_OPTION:
	    	fc.setFileSelectionMode(JFileChooser.FILES_AND_DIRECTORIES);
	    	
	    	
	   
	    	
	    	
	    	File selectedfile = fc.getSelectedFile().getAbsoluteFile();
	    	
	 

			 System.out.println(selectedfile);
			
			 
			 Runtime rt = Runtime.getRuntime();
			 String cmd = "gcr.exe "+selectedfile+" i "+GUI.names+"";
			 System.out.println(cmd);
		    	Process pr = rt.exec(cmd);
		    	
		
		    	
			 
	      break;
	    case JFileChooser.CANCEL_OPTION:
	      System.out.println("Bye!");
	      System.exit(0);
	      break;
	    case JFileChooser.ERROR_OPTION:
	      System.out.println("Error");
	      break;
	
		
		
	
	       
	        
		
	}

}
*/


	}
